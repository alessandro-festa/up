apiVersion: v1
kind: Service
metadata:
  name: suse-ai-up-minimal
  labels:
    app: suse-ai-up
spec:
  type: ClusterIP
  ports:
  - port: 8911
    targetPort: 8911
    protocol: TCP
    name: http
  selector:
    app: suse-ai-up